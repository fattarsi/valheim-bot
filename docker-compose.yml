version: "3.9"

services:
  valheim-bot:
    image: valheim/valheim-bot:latest
    container_name: valheim-bot
    restart: unless-stopped
    environment:
      - DISCORD_WEBHOOK=${DISCORD_WEBHOOK}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

